run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gofmt
    - goimports
    - revive

# No 'golint' or 'version' property. This config is compatible with golangci-lint v1.64+.

